# Experiments API

The high-level pipelines the CLI is built on. `CaptureExperiment` is what `cap capture` /
`cap run` drive; `PatchingExperiment` is what `cap patch` drives.

## Capture

::: cap.experiments.capture.CaptureExperiment

## Patching

::: cap.experiments.patch.PatchingExperiment
